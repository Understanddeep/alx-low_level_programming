#include "main.h"
/**
 * _abs - A cfile to compute the absolute value of an integer
 * @i: is the integer to consider
 * Return: absolute value of integer
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	return (-i);
}
